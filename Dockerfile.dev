# 基于 Node 18 镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 安装基础工具
RUN apk add --no-cache git

# 复制 package 相关文件
COPY package*.json ./

# 安装依赖
RUN npm install --registry=https://registry.npmmirror.com

# 复制所有项目文件
COPY . .

# 暴露 Vite 默认端口 3000
EXPOSE 3000

# 启动开发服务器
CMD ["npm", "run", "dev"]